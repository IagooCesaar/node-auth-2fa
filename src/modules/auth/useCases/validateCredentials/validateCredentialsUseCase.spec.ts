describe("Validate Credentials Use Case", () => {
  it("Should be able to validate a user's email and password", () => {
    // receber código temporário de login para aguardar autenticação 2 etapas
  });

  it("Should not be able to validate with inexistent email", () => {
    //
  });

  it("Should not be able to validate a incorrect password", () => {
    //
  });
});
